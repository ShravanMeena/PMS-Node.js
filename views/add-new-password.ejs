<%- include('header') %>


<div class="container">
  <!-- Default form login -->
<form class="text-center border border-light p-5  col-lg-4 offset-lg-4" action="/add-new-password" method="POST">
<%if(success !=''){%>
<p style="color: green;"> <%= success %> </p>
<%}%> 
    <!-- <p class="h4 mb-4">Sign in</p> -->
          <select class="browser-default custom-select" name="pass_cat">
            <option selected>Choose Password Category</option>
            <% if(records.length > 0){
              records.forEach(function(row){
           %>
            <option value="<%= row.password_category %>"><%=row.password_category %></option>
           <%})}%> 
           
          </select>
          <br>
    <div class="form-group">
      <input type="text" class="form-control" id="exampleInputPassword1" name="project_name" placeholder="Enter Project Name">
    </div>
   <textarea rows="5" id="editor1" name="pass_details" placeholder="Your Password Details"></textarea>
    <!-- Sign in button -->
    <button class="btn btn-info btn-block my-4" type="submit">Add </button>

    <!-- Register -->
    <p>Not a member?
        <a href="">Register</a>
    </p>

 
</form>
<!-- Default form login -->
</div>
<script>
  CKEDITOR.replace('editor1');
</script>

<%- include('footer') %>
